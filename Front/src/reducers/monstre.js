

const initialState = {
  name: 'Monster',
  experience: 0,
  level: 0,
  life: 0,
  strength: 10, 
  endurance: 0, 
  dexterité: 0,
};

const monstre = (state = initialState, action = {}) => {
  switch (action.type) {
   
    
    default:
      return state;
  }
};

export default monstre;
