

const initialState = {
  name: 'Minage',
  type: 'minerai',
  quantité: 0,
  level: 0,
  currentType: 'fer'
};

const craft = (state = initialState, action = {}) => {
  switch (action.type) {
    
    
    
    default:
      return state;
  }
};

export default craft;
